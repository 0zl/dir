html
  head
    title Dir - Rygnaizer Server
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    style(type="text/css").
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }
      
      h4, a {
        font-family: "monospace" !important;
      }
      
      a {
        display: block;
        text-decoration: none;
        color: rgba(0, 0, 0, .5);
        margin-left: 10px;
        margin-bottom: 3px;
      }
      
      img {
        width: 100%;
      }
      
  body
    img(src="/r.png" alt="<rygna>")
    br
    br
    for v in data
      a(href=`/?f=${v.name}` target="__blank") › (#{v.size}) #{v.name}